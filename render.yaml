services:
  - type: web
    name: your-app-name
    env: php
    buildCommand: "./deploy.sh"
    startCommand: "php artisan serve --host=0.0.0.0 --port=10000"
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"